import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-map',
  templateUrl: './map.component.html',
  styleUrls: ['./map.component.css']
})
export class MapComponent {
  /*map: google.maps.Map | undefined;

  constructor() { }

  ngOnInit(): void {
    // Initialisation de la carte
    this.map = new google.maps.Map(document.getElementById('map') as HTMLElement, {
      center: { lat: 40.7128, lng: -74.0060 }, // Coordonnées de New York (par défaut)
      zoom: 12 // Zoom par défaut
    });

    // Ajouter un marqueur pour une localisation spécifique
    const marker = new google.maps.Marker({
      position: { lat: 40.7128, lng: -74.0060 }, // Coordonnées de New York (par exemple)
      map: this.map,
      title: 'Hello World!' // Titre du marqueur
    });
  }
*/
}
